package java.harsha.assignment;

import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class AllLinksSameReverse {
	
	
/*	public static List<WebElement> getAllLinksSame(WebDriver driver,By by)
	{
		for(int i=0;i<driver.findElements(by)).size();i++)
        {
        	String linkText=driver.findElements(by).getText(i);
        }
		return linkText;
	}
*/
}
